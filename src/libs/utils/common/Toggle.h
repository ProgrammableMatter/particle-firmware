#ifndef __Toggle_h__
#define __Toggle_h__

#include "ToggleMacros.h"


/**
 * toggle macros - also generate a pot of toggle macros
 */
#define ToggleB0 \
    TOGGLEINVARIANT(B, 0)
#define InitToggleB0 \
    TOGGLEINIT(B, 0)

#define ToggleB1 \
    TOGGLEINVARIANT(B, 1)
#define InitToggleB1 \
    TOGGLEINIT(B, 1)

#define ToggleB2 \
    TOGGLEINVARIANT(B, 2)
#define InitToggleB2 \
    TOGGLEINIT(B, 2)

#define ToggleB2 \
    TOGGLEINVARIANT(B, 2)
#define InitToggleB2 \
    TOGGLEINIT(B, 2)

#define ToggleB3 \
    TOGGLEINVARIANT(B, 3)
#define InitToggleB3 \
    TOGGLEINIT(B, 3)

#define ToggleB4 \
    TOGGLEINVARIANT(B, 4)
#define InitToggleB4 \
    TOGGLEINIT(B, 4)

#define ToggleB5 \
    TOGGLEINVARIANT(B, 5)
#define InitToggleB5 \
    TOGGLEINIT(B, 5)

#define ToggleB6 \
    TOGGLEINVARIANT(B, 6)
#define InitToggleB6 \
    TOGGLEINIT(B, 6)

#define ToggleB7 \
    TOGGLEINVARIANT(B, 7)
#define InitToggleB7 \
    TOGGLEINIT(B, 7)


#endif
